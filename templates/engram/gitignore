# Engram sync data — track chunks + manifest, ignore rest
.engram/*
!.engram/chunks/
!.engram/chunks/**
!.engram/manifest.json

# Engram local database files (generated by engram daemon)
*.db
*.db-journal
*.db-wal
*.db-shm
